import styled from 'styled-components'

export const Nav = styled.nav`
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 0rem 1rem;
  .p-menuitem-link {
    border-color: #FFF;
  }
  .icon {
    font-size: 3rem;
    color: red
  }
`